spring.application.name=acc-backend

# 数据库连接配置（MySQL）
spring.datasource.url=jdbc:mysql://localhost:3306/acc?useSSL=false&characterEncoding=utf8&serverTimezone=Asia/Shanghai
spring.datasource.username=root
spring.datasource.password=151505
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate（如后续使用可调整）
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false

# Web 服务端口
server.port=8080

# sa-token 基本配置（可按需扩展）
sa-token.token-name=satoken
sa-token.timeout=2592000
sa-token.is-log=false

